# CV-Tracker — dépendances directes (pas un freeze venv)
# Python 3.10+
#
# Install typique :
#   pip install -r requirements.txt
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu130
# (adapte l'URL CUDA : cu118 / cu124 / cu130 — voir https://pytorch.org/get-started/locally/)

# --- Runtime pipeline (main.py) ---
dxcam==0.3.0
numpy>=1.26,<3
opencv-python>=4.8
ultralytics==8.4.92
pyserial==3.5
PyYAML>=6.0

# --- Annotation manuelle (LabelImg) ---
labelImg==1.8.6

# --- Export TensorRT (optionnel) ---
# Installer le kit / wheels TensorRT alignés sur ta CUDA, puis :
#   python scripts/export_engine.py
# Ne pas pinner tensorrt_* ici : trop lié à la machine.
